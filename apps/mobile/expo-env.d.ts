/// <reference types="expo/types" />

// NOTE: This file should not be edited and should be in your .gitignore... but
// keeping it tracked here is intentional: it makes `process.env.EXPO_PUBLIC_*`
// type-check in CI (no `expo start`/prebuild runs there to regenerate it).
